at the root folder start docker by using :- docker compose start
at the root folder stop docker by using :- docker compose stop

at server root use command :- npm run server to run server seprately
at client root use command :- npm run client to run dev seprately